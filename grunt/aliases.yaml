default:
- 'server'

test:
- 'jshint'
- 'build'

build:
- 'clean:dist'
- 'jshint'
- 'sass:dist'
- 'autoprefixer:dist'
- 'copy'
- 'cssmin:dist'

deploy:
- 'build'
- 'gh-pages'

server:
- 'clean:dev'
- 'sass:dev'
- 'autoprefixer:dev'
- 'connect:dev'
- 'open:dev'
- 'watch'

server-dist:
- 'build'
- 'connect:dist'
- 'open:dist'
- 'watch'
