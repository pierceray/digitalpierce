default:
- 'server'

test:
- 'jshint'

build:
- 'clean:dist'
- 'jshint'
- 'sass:dist'
- 'autoprefixer:dist'
- 'copy'

deploy:
- 'build'
- 'gh-pages'

server:
- 'clean:dev'
- 'sass:dev'
- 'autoprefixer:dev'
- 'connect:dev'
- 'open:dev'
- 'watch'

server-dist:
- 'build'
- 'connect:dist'
- 'open:dist'
- 'watch'
